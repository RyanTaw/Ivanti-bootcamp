kind: Service
apiVersion: v1
metadata:
  name: containerapp-itemsapi-svc
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: containerapp-itemsapi
    tier: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: containerapp-itemsapi-deploy
  namespace: bootcamp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: containerapp-itemsapi
      tier: backend
  template:
    metadata:
      labels:
        app: containerapp-itemsapi
        tier: backend
    spec:
      containers:
        - name: containerapp-itemsapi
          image: avantiavantibootcampacr.azurecr.io/containerapp-itemsapi:latest
          env:
            - name: MyDbContext
              value: "Server=tcp:avanti-bootcamp-sql.database.windows.net,1433;Initial Catalog=bootcampdb;Persist Security Info=False;User ID=sqladminuser;Password=Ryanpassword123!;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
          # env:
          #   - name: ConnectionStrings__DefaultConnection
          #     valueFrom:
          #       secretKeyRef:
          #         name: sql-connection-secret
          #         key: SQL_CONNECTION_STRING
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: containerapp-itemsapi-scaler
  namespace: bootcamp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: containerapp-itemsapi-deploy
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 20